a = 6;
b = 7;

var resposta = a > b ? "maior" : "menor";

var resposta2 = a > b ? "maior" : a < b ? "menor" : "igual";

// Dias da semana com operador ternário

var diaSemana =
  dia === 1
    ? "Domingo"
    : dia === 2
    ? "Segunda"
    : dia === 3
    ? "Terça"
    : "Outro dia da semana";
